name: Test Contribs

on:

#  workflow_call:
#    inputs:
#      javaVersion:
#        description: "Java Version"
#        type: string
#        required: true
#      repository:
#        description: "Repository name with owner."
#        type: string
#        required: true
#      ref:
#        description: "The branch, tag or SHA to checkout"
#        type: string
#        required: true

  workflow_dispatch:
    inputs:
      javaVersion:
        description: "Java Version"
        required: true
        default: '11'
      repository:
        description: "Repository name with owner."
        required: true
        default: 'clojure/clojure'
      ref:
        description: "The branch, tag or SHA to checkout"
        required: true
        default: 'master'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: ${{ inputs.javaVersion }}

      - name: Checkout Clojure Source Code
        uses: actions/checkout@v4
        with:
          repository: ${{ inputs.repository }}
          ref: ${{ inputs.ref }}
          path: 'clojure-source'

      - name: Build Clojure from Source
        run: |
          cd clojure-source
          mvn install

      - name: Checkout Core.Async Source Code
        uses: actions/checkout@v4
        with:
          repository: clojure/core.cache
          ref: 'master'
          path: 'contrib-source'

      - name: Checkout Integration Test Source Code
        uses: actions/checkout@v4
        with:
          repository: JarrodCTaylor/clojure-integration-tests
          ref: 'main'
          path: 'test-source'

      - name: Determine latest snapshot version
        run: |
          chmod +x ./test-source/scripts/find-latest-snapshot.sh
          ./test-source/scripts/find-latest-snapshot.sh

      - name: Run Tests
        run: |
          cd /home/runner/work/clojure-integration-tests/clojure-integration-tests/contrib-source
          mvn -ntp -B -Dclojure.version=${{ env.LATEST_SNAPSHOT }} clean test
